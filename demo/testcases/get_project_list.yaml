#   config文件是设置环境变量，方便后续使用
config:
    # name指定当前用例集名称
    name: "这是获取项目数据的测试用例"

    # variable设置变量
#    variables:
#        foo1: config_bar1
#        foo2: config_bar2
#        expect_foo1: config_bar1
#        expect_foo2: config_bar2

    # headers 指定请求头参数
    headers:
        Content-Type: application/json


teststeps:
-
    # 当前用例，名称
    name: '项目数据接口'
    request:
        # 请求方法
        method: get
        # 请求路径
        url: http://127.0.0.1:8000/projects/
        # 参数
        params:
            search: zxxxx2
    # 断言
    validate:
        - eq: ["status_code", 200]

